"""elvideo — Path B (Gemini-native) shot-level video understanding.

Turn one ≤10-min video into a shot-level text index (``footage_index.json``) that an agent can
later reason over, running locally on the free tier.

Spec: ``docs/IDEA.md``. Orientation for a cold start: ``.claude/CLAUDE.md``.
"""

from __future__ import annotations

__version__ = "0.1.0"
